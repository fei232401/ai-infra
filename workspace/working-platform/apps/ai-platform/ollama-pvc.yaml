# Ollama 模型数据 PVC — 单独文件管理（保护模型数据，避免随 stack 重装丢失）
# 存储类: local-path (k3s 内置)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: ai-platform
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
