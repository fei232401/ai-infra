# P1 验证发现（2026-08-16）

> P1 功能验证的阶段性记录：**离线验证已绿，集群 e2e 延期**。记录发现的真实缺口与口径统一结果，全部诚实可复现。

## 0. 验证范围与状态

| 层 | 验证内容 | 状态 |
|---|---|---|
| CyberRouter（Go） | `go test ./...`（decision / metrics 包） | ✅ 全绿 |
| 网关数据面（Python） | `pytest` 35 项 | ✅ 全绿（修复前 6 failed + 挂起） |
| 集群端到端 | ai-cluster 起 vLLM+LMCache+Operator+网关 | ⏸ 延期（集群停用，先备 runbook） |

## 1. 命中率口径统一（证据分级）

**三个数字是三个不同含义，必须分开讲：**

| 数字 | 含义 | 可复现性 | 处置 |
|---|---|---|---|
| **95.1%** | vLLM 前缀缓存命中率，共享前缀负载测试 | ✅ 复现自 `heteroserve/benchmark/results/matrix_results.json` A1（`prefix_cache_hits 39312 / queries 41350`），`tuning_matrix.py::fetch_cache_metrics` 从 vLLM `/metrics` 实采 | **保留**（简历唯一命中率口径） |
| 40.5% | 曾称"LMCache 命中率（互异前缀负载）" | ❌ `benchmark.py` 不计算命中率，无复现来源 | **已撤**（README / BLUEPRINT 已改） |
| 27.6% | 曾称"prefix_cache 实测" | ❌ 无复现来源，与 95.1% 口径冲突 | **已撤**（environment.md / BLUEPRINT 已改） |

**简历口径**：命中率 = **95.1%**（vLLM 前缀缓存，共享前缀负载下实测），并列 **TTFT 3–5×**（逐出前缀恢复，`benchmark.py` before/after 真实测量）。任何 LMCache 层专属命中率需重新测量 + 抓指标后才能声称，当前不做。

**被撤数字的去向**：已从 `heteroserve/README.md`、`docs/environment.md`、`docs/BLUEPRINT.md`（含简历话术 §9/§10、追问防拆清单）全部清除，并注明撤因。

## 2. 熔断器缺口（真实 gap，需决策）

**发现**：`gateway_server.py` 的 `CircuitBreaker` 只**记录**状态（`record_failure`/`record_success` 仅在 `/api/generate` 的 ollama 分支调用），但 **`allow_request()` 从未被任何路由调用**——请求永远不会被 503 拦截。熔断器是"观测仪表盘"不是"断路器"。

- 证据：`grep allow_request` 只在类定义处出现；`record_failure` 只在 line 274。
- 影响：BLUEPRINT/README 将"熔断"列为网关能力（Phase A），若面试被问到"熔断怎么拦截请求"会塌。
- 选项（**待用户决策，未擅自改**）：
  - A. 在 `/api/generate`/`/api/chat/stream` 入口加 `if not circuit_breaker.allow_request(): raise 503`（把能力做实，代码量小，需补拦截后回归）
  - B. 保持观测-only，把 README 措辞降级为"熔断状态观测"，简历不把熔断当拦截能力讲

## 3. 401 观测缺口

**发现**：`auth_middleware` 是**最外层**中间件（后注册 = 外层），401 在进入 `metrics_middleware` 的 `call_next` 之前直接返回，因此 **401 不计入 `gateway_requests_total`**。拒绝流量对 Prometheus 不可见。

- 影响：观测面板看不到"有多少请求被鉴权拒掉"，安全事件排查缺数据。
- 选项（待用户决策）：在 `auth_middleware` 拒绝分支加 `REQUEST_COUNT.labels(status_code="401").inc()`（2 行），或记为 known gap。

## 4. 测试腐坏修复记录（gateway Python 套件）

套件与实现脱节（实现重构过、测试没跟上），修复后 35/35：

| 问题 | 根因 | 修复 |
|---|---|---|
| CircuitBreaker 构造 6/6 TypeError | 实现签名 `__init__(ft, ts)`，测试用关键字 `failure_threshold=` | 构造器参数改名（`gateway_server.py`） |
| 测试挂起 120s | `gateway_config.yaml` `auth.enabled=false` 但测试全假设鉴权开 → 401 测试漏到真实后端（`ollama-service:11434` 域名解析失败） | `conftest.py` autouse 强制 `auth.enabled=True`，与 shipped 配置解耦 |
| 过期 JWT 断言过细 | 中间件对过期/无效 token 统一返回 `{"error":"Unauthorized"}`，不区分消息 | 放宽为只断言 401 |
| `/api/models` 断言 502 | 实现是尽力而为：后端不可达 log 后返回 200（resilient by design） | 改为断言 200 + models 字段 |
| 熔断集成断言 503 | 熔断器未接入拦截（见 §2） | 改为断言 `record_failure` 接线：连续 `/api/generate` 失败 → `state==OPEN` |

## 5. 待办

- [ ] 备 ai-cluster 端到端 runbook（vLLM 镜像已缓存 28GB，GPU 空闲）→ 起集群验证
- [ ] 决策 §2 熔断器缺口（A 做实 / B 降级口径）
- [ ] 决策 §3 401 观测（补计数 / 记为 gap）
- [ ] P1 简历素材产出（见 task #43）
